<h1>Creating a new List</h1>

<%= simple_form_for([@list]) do |f| %>
<%= f.input :name %>
<%= f.input :photo, as: :file %>
<%= f.submit %>
<% end %>

<%= link_to "Discard & Return to Lists", lists_path %>
